name: Manual QA Approval

on:
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:  # Allows manual triggering

jobs:
  manual-approval:
    name: "Manual QA Approval"
    runs-on: ubuntu-latest
    environment: QA-Approval  # This enforces QA team approval

    steps:
      - name: Wait for QA Approval
        run: echo "Waiting for QA to approve..."
